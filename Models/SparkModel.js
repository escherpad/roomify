/**
 * Created by ge on 5/13/15.
 */
if (typeof define !== 'function') {
    var define = require('amdefine')(module);
}
define(['underscore', 'async', '../api-server/models/SchemaModels', '../api-server/rolesHelper',  'wascally'],
    function (_, async, SchemaModels, rolesHelper,  messageQue) {
        "use strict";
        var Spark = SchemaModels.Spark;
    });
